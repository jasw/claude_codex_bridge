# agentroles.code_reviewer

Draft accepted RolePack for independent node-level code review.

The code reviewer verifies one coder result against the assigned contract and
rejects missing evidence, hidden fallback, degradation, and scope shrinkage.
